import { SectionHeading } from "@/components/SectionHeading"; import { courses } from "@/lib/data"; export default function Page(){return <section className="section"><div className="container page-shell"><SectionHeading eyebrow="Teaching" title="Courses" text="Undergraduate and graduate teaching hub for syllabi, lectures, labs, datasets, readings and assessment."/><h2 className="subheading">Undergraduate</h2><div className="course-grid">{courses.undergraduate.map(c=><article className="course-card" key={c.name}><span>Undergraduate</span><h3>{c.name}</h3><p>{c.focus}</p></article>)}</div><h2 className="subheading">Graduate</h2><div className="course-grid">{courses.graduate.map(c=><article className="course-card" key={c.name}><span>Graduate</span><h3>{c.name}</h3><p>{c.focus}</p></article>)}</div></div></section>}
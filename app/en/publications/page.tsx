import { SectionHeading } from "@/components/SectionHeading"; import { publications,thesis,links } from "@/lib/data"; export default function Page(){return <section className="section"><div className="container page-shell"><SectionHeading eyebrow="Publications" title="Scientific publications" text="Verified DOI identifiers are displayed when available; complete records are maintained in academic profiles."/><div className="pub-list">{publications.map(p=><article className="pub-row" key={p.title}><span className="pub-year">{p.year}</span><div><h3>{p.title}</h3><p>{p.authors}</p><em>{p.venue}</em>{p.doi?<div className="doi"><a href={p.url}>DOI {p.doi} ↗</a></div>:<div className="doi muted">DOI not displayed until verified from a primary source</div>}</div></article>)}</div><div className="thesis-box"><span>Master's Thesis · USP · 2020</span><h3>{thesis.title}</h3><a href={thesis.url}>DOI {thesis.doi} ↗</a></div><div className="actions"><a className="button primary" href={links.scholar}>Google Scholar</a><a className="button secondary" href={links.orcid}>ORCID</a><a className="button secondary" href={links.scopus}>Scopus</a></div></div></section>}